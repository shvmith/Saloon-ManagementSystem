
// api config

const API_CONFIG = {
  BASE_URL: "http://localhost:7001", // Base URL for your backend
  ENDPOINTS: {
   
    INVENTORY: "/api/v1/inventory", // Added inventory endpoint
    APPOINTMENTS: "/api/v1/appoiment", // Added appointments endpoint
    SERVICES: "/api/services",
    PACK: "/api/v1/package",
    FEEDBACK: "/api/v1/feedback", // Added feedback endpoint
  },
};

export default API_CONFIG;